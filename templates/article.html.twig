{% extends 'partials/base.html.twig' %}

{% block content %}
	<h1>{{ header.title }}</h1>
	<div class="content">
		<small><i>{{ page.date | date('d.m.Y') }}</i></small>
		{{ page.content|raw }}
	</div>
{% endblock %}
